<h1>Produtos</h1>

<div class="products">
    {{#each products}}
        <div class="product">
            <h3>{{this.name}}</h3>
            <img src="{{this.image}}" alt="{{this.title}}">
            <p>R$ {{this.price}}</p>
        </div>
    {{/each}}
</div>